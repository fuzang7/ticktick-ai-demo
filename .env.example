# TickTick (Dida365) API Configuration
# Copy this file to .env and fill in your credentials

# REQUIRED: OAuth2 credentials from TickTick Developer Portal
# Register at: https://developer.dida365.com/openapi
TICKTICK_CLIENT_ID=your_client_id_here_32_characters_long
TICKTICK_CLIENT_SECRET=your_client_secret_here
TICKTICK_REDIRECT_URI=http://localhost:8080/callback

# OPTIONAL: Inbox Project ID
# Leave empty if you don't need inbox-specific operations
# To find your inbox ID:
# 1. Use the TickTick API to list all projects
# 2. Look for project with type "INBOX"
TICKTICK_INBOX_ID=

# Notes:
# 1. Keep this file secret - never commit .env to version control
# 2. The redirect URI must exactly match what's registered in TickTick Developer Portal
# 3. Client ID is typically 32 characters long
# 4. Run refresh_token.py to authenticate and get an access token